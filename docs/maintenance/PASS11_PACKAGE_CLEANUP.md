# v0.5.17 DEV PASS11 package cleanup

This maintenance pass deliberately removes non-runtime historical bulk from the active app package while preserving the exact pre-cleanup source in the verified **v0.5.17 dev pass10 ZIP**.

Removed categories:

- superseded `.pre_*` source/test/document snapshots;
- historical design concept/reference PNGs;
- historical research screenshot evidence whose conclusions are already indexed in canonical research text/data;
- binary source-archive copies (including the embedded RAR and duplicate Mentor/tier source PNGs);
- superseded v0.5.15/v0.5.16 handoff duplicates and dev-pass1–9 root validation notes.

Preserved:

- all active runtime JS/CSS/HTML;
- all scanner runtime references and manifests;
- scanner regression fixtures;
- current canonical game/research text/data indexes;
- current v0.5.17 recovery handoff + continuity mirror;
- pass10 baseline validation and the new pass11 validation;
- all calibrated decision-engine tests.

Package file count before cleanup: **825**  
Package bytes before cleanup: **37,290,874**  
Files removed: **128**  
Bytes removed: **13,586,833**  
Current file count at cleanup point: **697**  
Current bytes at cleanup point: **23,704,041**

No runtime or calibrated scoring asset was removed.


Final pre-package Pass11 working tree after validation/recovery updates: **700 files / 23,297,065 bytes**.
