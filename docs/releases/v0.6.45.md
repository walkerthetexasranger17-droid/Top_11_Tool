# v0.6.45 — Green-button parity

Focused visual-consistency follow-up. Every enabled green primary action now uses the exact computed surface treatment of the approved Individual Training active tab: the same translucent green/navy gradient, lime frame, bottom lime keyline/glow and light text. Team Plan active tabs are included so page-specific rules cannot drift. Disabled actions remain subdued. No football, optimiser, scanner, Mastercard or cloud logic changed.
