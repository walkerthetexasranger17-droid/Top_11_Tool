# v0.5.16 — Release Hardening Pass

**Release freeze:** 2026-09-16

v0.5.16 is a code-quality and deployment-hardening release built directly on the release-frozen v0.5.15 calibration. **No Formation, Tactics, Mentor, Set Piece, Training, condition-drain or all-in-one decision score changed.** The Strategy model fingerprint therefore remains `companion-strategy-v2-own-squad-runtime-v0515`.

## Hardening fixes

- Corrected eight inline hero-image URLs so static hosting under a subdirectory resolves assets inside the app rather than at the domain root.
- Hardened service-worker offline runtime fallback with query-insensitive cache matching. This lets precached `js/css/manifest` files satisfy requests carrying the release cache-buster when the network is unavailable.
- Enforced the existing scanner skill contract of **0–520** at save time and in the Gemini core response schema.
- HTML-escaped account provider/MFA values before they enter account-page `innerHTML`.
- Synchronized stale Windows launcher and cloud diagnostic version labels.
- Archived the malformed obsolete `tests/calibration_probe.js` scratch probe as non-executable historical evidence; it was never part of runtime or the release gate.

## Preserved calibrated system

The complete own-squad path remains unchanged:

`eligible squad → Formation/XI → Tactics → Set Pieces/Captain → Mentor → final Team Plan → Training context`

Opponent information remains permanently out of scope. Shadow Striker remains the sole player-facing SA identity; internal `LongShots` remains provenance/import compatibility only. Mixed Medium/High live tactic-drain arithmetic remains unresolved and is not guessed.

## Release identity

- public/runtime version: `0.5.16`;
- calibrated Strategy model: `companion-strategy-v2-own-squad-runtime-v0515` (unchanged);
- Team Plan schema: v6 (unchanged);
- service-worker cache: `te-v0-5-16`;
- runtime asset cache-buster: `r=0516`.
