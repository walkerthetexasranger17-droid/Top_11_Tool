# v0.6.48 — UPDATE PLAYER title asset restore

- Restores the exact user-approved 1747×900 UPDATE PLAYER title PNG byte-for-byte.
- Removes the v0.6.47 resized/recompressed derivative from the runtime package.
- Uses CSS-only sizing to match the established Add Player visible header footprint.
- Keeps v0.6.47 Login, glass, Team Plan changing-room background and first-paint background stability fixes.
- No football/decision/scanner logic changes.
