# Top Eleven Tool v0.5.0

**Phase start:** 13 September 2026  
**Internal runtime:** `0.5.0-r2`

v0.5.0 starts the Team Plan evidence/research phase from the proven v0.4.18 scanner baseline.

## r1 — Set Pieces provenance + research baseline

### Research foundation

- Verified the supplied full Windows game package as the exact previously researched v27.3.0.0/build-30527 IL2CPP build by matching both `GameAssembly.dll` and `global-metadata.dat` SHA-256 hashes.
- Added `docs/development/V050_TEAM_PLAN_EVIDENCE_LEDGER.md` separating GAME FACT, LIVE FACT, COMPANION LOGIC, community evidence and UNRESOLVED behaviour.
- Confirmed current client Set Piece Taker storage/edit/save paths for Captain, left/right corners, left/right free kicks, penalty kicker and ordered penalty takers.
- Confirmed `PenaltyKickSpecialist`, `FreeKickSpecialist`, `CornerSpecialist` and `SetPieceTaker` remain current Special Ability enum entries.
- Confirmed no authoritative automatic Captain-selection formula has yet been recovered. Captain stays manual.

### Set Pieces correction

- Replaced flat `teamplan:setpieces:v3` state with provenance-aware `teamplan:setpieces:v4` state.
- Each slot now records whether it is an automatic recommendation, manual override, or migrated legacy value.
- Automatic non-Captain recommendations recalculate against the current XI instead of remaining stale indefinitely.
- Manual user assignments survive Team Plan recalculation.
- Existing v3/v2 assignments migrate non-destructively as `legacy` so the app does not silently overwrite unknown prior user intent.
- Added **Refresh Recommendations** to deliberately reset all non-Captain slots to the current Top Eleven Tool recommendation.
- Captain is explicitly described as manual/unresolved instead of appearing to be a failed recommendation.

### Frozen areas

- Scanner v12 logic/assets: unchanged.
- Formation engine/layout: unchanged.
- Tactics recommendation model: unchanged in r1; research continues.
- Mentor recommendation model: unchanged in r1; a level/effect-state defect has been documented for the next Mentor pass.

### Regression status

- Core deterministic suite: **270 assertions PASS**.
- Static checks: PASS.
- Package integrity: PASS.
- Cloud hydration/navigation contract: PASS.
- v0.4.18 scanner native-resolution and playstyle-reference contracts: PASS unchanged.


## r2 — Tactics evidence correction

- Exact build-30527 drain math remains unchanged.
- Removed hand-authored Approach weighting from the ten non-Mentality tactic dimensions.
- Added current-game semantic tie logic and playstyle/SA semantic tie-breaks without claiming private match-engine weights.
- Marking is no longer inferred from our own defenders; current game guidance shows it is opponent-dependent.
- Equal lane support now uses neutral/broad focus choices instead of arbitrary enum-first flanks.
- Tactics model is now `30527-drain-fit-v2`.
- Core suite: 276 assertions.
