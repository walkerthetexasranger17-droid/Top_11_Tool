# v0.6.12 — Browser QA / True Portrait Fit Pass 12

Adds a policy-safe offline real-Chromium viewport harness and fixes the portrait overflow it exposed. The previous widened-plus-CSS-zoom portrait canvas is replaced with a genuine 100%-viewport compact layout, with only measured portrait-tablet workspace reflows. No football/scanner/calibration logic or authoritative extracted game assets changed.
