# Top Eleven Tool v0.4.13

## Scanner reference rebuild

- Replaced all legacy playstyle identity/level composite indexes with the approved exact atlas/code-derived playstyle state pack.
- Bundles 20 playstyles with 13 valid states each (260 exact PNGs).
- Replaced Special Ability references with the supplied 19 coloured icons only.
- Removed the gold/boosted Special Ability reference path completely.
- Removed legacy `assets/playstyles/` and `assets/abilities/` icon packs; profile UI now uses the exact reference assets.
- Playstyle identity is matched against exact Standard references, then state/level is matched against the 13 exact states for that same identity.
- Standard is now a valid selectable/detectable level.
- Ready, Boosted and Wrong Position are separated from the underlying tier during scanner classification.
- Added per-reference SHA-256 integrity checks.
