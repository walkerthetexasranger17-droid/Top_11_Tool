# Archived scratch probes

`calibration_probe.legacy-broken.txt` is an obsolete exploratory calibration scratch file recovered in a syntactically malformed state. It was never part of the production runtime or release regression gate. It is retained as historical evidence only and deliberately does not use a `.js` extension so automated JavaScript checks cannot mistake it for an executable test.
