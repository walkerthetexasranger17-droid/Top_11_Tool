(() => {
  const TE=window.TE5=window.TE5||{};const B=TE.BibleData,D=TE.Data,STRAT=TE.Strategy;
  if(!B||!D||!STRAT)throw new Error('bible-data.js, data.js and strategy-logic.js must load before tactics-engine.js');
  const MODEL_VERSION='30527-drain-fit-v5-calibrated-v0515';
  const DIMENSIONS=['passing','shooting','focus','cross','lost','won','mentality','marking','pressing','backLine','tackling'];
  const SEARCH_DIMS=DIMENSIONS.filter(x=>x!=='mentality');
  const DRAIN_RANK={Low:0,Medium:1,High:2};
  const LIVE_DRAIN_MAX=null;
  const DRAIN_PROFILE_LEGACY='legacyDefault';
  const DRAIN_PROFILE_LIVE='liveObserved';
  function option(dim,key){return B.TACTICS[dim]?.find(x=>x.key===key)||null;}
  function resolveDrainContribution(defaultContribution,override){
    if(override==null)return defaultContribution;
    let raw=override,kind='auto';
    if(typeof override==='object'&&override!==null){
      if(override.intensity!=null){raw=override.intensity;kind='intensity';}
      else if(override.contribution!=null){raw=override.contribution;kind='contribution';}
      else if(override.value!=null)raw=override.value;
      else return defaultContribution;
    }
    if(typeof raw==='string'){
      const key=raw.trim().toLowerCase();
      if(key==='low')return 0;if(key==='medium')return 5;if(key==='high')return 7;
    }
    const n=Number(raw);
    // Native TacticsDrainOverrides stores nullable ConditionDrainIntensity enum values:
    // Low=0, Medium=1, High=2. Keep legacy/direct contribution inputs 0/5/7 too.
    if(kind==='intensity'||(kind==='auto'&&(n===1||n===2)))return n===0?0:n===1?5:n===2?7:defaultContribution;
    return [0,5,7].includes(n)?n:defaultContribution;
  }
  function calculateLegacyDrain(values,overrides={}){
    let raw=15;
    for(const dim of DIMENSIONS){
      const o=option(dim,values?.[dim]);if(!o)throw new Error(`Missing tactic ${dim}`);
      const override=overrides?.[dim]?.[o.key];raw+=resolveDrainContribution(o.drain,override);
    }
    const normalized=raw/100,drainClass=normalized>.65?'High':normalized>.40?'Medium':'Low';
    return{rawScore:raw,normalized,drainClass,classResolved:true,profile:DRAIN_PROFILE_LEGACY};
  }
  function liveDrainContribution(dim,key){
    const n=Number(B.LIVE_DRAIN?.[dim]?.[key]);
    if(!Number.isFinite(n)||n<0||n>2)throw new Error(`Missing/invalid live drain intensity ${dim}.${key}`);
    return n;
  }
  function liveDrainCounts(values){
    let mediumCount=0,highCount=0;
    for(const dim of DIMENSIONS){const o=option(dim,values?.[dim]);if(!o)throw new Error(`Missing tactic ${dim}`);const intensity=liveDrainContribution(dim,o.key);if(intensity===1)mediumCount++;else if(intensity===2)highCount++;}
    return{mediumCount,highCount,lowCount:DIMENSIONS.length-mediumCount-highCount};
  }
  function classifyLiveObserved(counts){
    // Only pure-class transition anchors are currently proven by live UI testing.
    // Mixed Medium/High packages remain unresolved until further observations recover
    // the hidden numeric intensity weights / server threshold configuration.
    const m=counts?.mediumCount??0,h=counts?.highCount??0;
    if(h===0){if(m>=9)return'High';if(m>=3)return'Medium';return'Low';}
    if(m===0){if(h>=6)return'High';if(h>=2)return'Medium';return'Low';}
    return null;
  }
  function liveDrainDominatesCheaper(a,b){
    // Safe partial order: fewer-or-equal Medium choices AND fewer-or-equal High choices.
    // This never assumes a numeric exchange rate between Medium and High intensities.
    return a&&b&&a.mediumCount<=b.mediumCount&&a.highCount<=b.highCount&&(a.mediumCount<b.mediumCount||a.highCount<b.highCount);
  }
  function calculateLiveDrain(values){
    const counts=liveDrainCounts(values),drainClass=classifyLiveObserved(counts);
    return{rawScore:null,relativeScore:null,normalized:null,...counts,drainClass:drainClass||'Unclassified',classResolved:!!drainClass,profile:DRAIN_PROFILE_LIVE};
  }
  function calculateDrain(values,overrides={},profile=DRAIN_PROFILE_LEGACY,liveThresholds=null){
    return profile===DRAIN_PROFILE_LIVE?calculateLiveDrain(values):calculateLegacyDrain(values,overrides);
  }
  function enumerate(callback,mentalityKey=null){let count=0;const dims=mentalityKey?SEARCH_DIMS:DIMENSIONS;const vals={};function rec(i){if(i===dims.length){if(mentalityKey)vals.mentality=mentalityKey;count++;callback?.({...vals});return;}const dim=dims[i];for(const o of B.TACTICS[dim]){vals[dim]=o.key;rec(i+1);}}rec(0);return count;}
  function drainChecksums({profile=DRAIN_PROFILE_LEGACY,liveThresholds=null}={}){const mk=()=>profile===DRAIN_PROFILE_LIVE?{Low:0,Medium:0,High:0,Unclassified:0}:{Low:0,Medium:0,High:0},all=mk(),perMentality={};enumerate(v=>all[calculateDrain(v,{},profile,liveThresholds).drainClass]++);for(const m of B.TACTICS.mentality){const c=mk();enumerate(v=>c[calculateDrain(v,{},profile,liveThresholds).drainClass]++,m.key);perMentality[m.key]=c;}return{total:Object.values(all).reduce((a,b)=>a+b,0),all,perMentality,profile};}
  function mean(a){const v=a.filter(Number.isFinite);return v.length?v.reduce((x,y)=>x+y,0)/v.length:0;}
  function lineupCompleteness(starters=[]){const missing=[];for(const s of starters){const role=D.normaliseRole(s?.assignedRole);if(!role){missing.push({playerKey:String(s?.player?.key||s?.player?.name||''),assignedRole:null,attributes:['assignedRole']});continue;}const attrs=D.applicableSkillsForRoles([role]),miss=D.missingSkills(s?.player?.skills,attrs);if(miss.length)missing.push({playerKey:String(s?.player?.key||s?.player?.name||''),assignedRole:role,attributes:miss});}return{complete:missing.length===0,missing};}
  function isKey(starter,attr){return (D.POSITION_WHITE[starter.assignedRole]||[]).includes(attr);}
  function metric(starters,attrs,{roles=null,lane=null}={}){let pool=starters;if(roles)pool=pool.filter(s=>roles.includes(s.assignedRole));if(lane)pool=pool.filter(s=>lane==='left'?s.y<250:lane==='right'?s.y>750:s.y>=250&&s.y<=750);let vals=[];for(const s of pool)for(const a of attrs){const v=D.skillValue(s.player?.skills,a);if(isKey(s,a)&&v!==null)vals.push(v);}if(!vals.length){for(const s of pool)for(const a of attrs){const v=D.skillValue(s.player?.skills,a);if(v!==null)vals.push(v);}}return mean(vals);}
  function lineupMetrics(starters){
    const m={};m.TechnicalBuild=metric(starters,['Passing','Creativity','Dribbling']);m.ShootingPower=metric(starters,['Shooting']);m.Finishing=metric(starters,['Shooting','Finishing']);m.WideAttack=metric(starters,['Crossing','Dribbling','Speed'],{roles:['DL','DR','ML','MR','AML','AMR']});m.AerialTarget=metric(starters,['Heading','Strength','Positioning'],{roles:['AMC','ST']});m.Transition=metric(starters,['Speed','Passing','Dribbling','Finishing'],{roles:['ML','MC','MR','AML','AMC','AMR','ST']});m.PressingUnit=metric(starters,['Fitness','Aggression','Tackling','Bravery'],{roles:['DL','DC','DR','DMC','ML','MC','MR','AML','AMC','AMR','ST']});m.DefensiveUnit=metric(starters,['Tackling','Marking','Positioning','Bravery'],{roles:['DL','DC','DR','DMC','ML','MC','MR']});m.LineControl=metric(starters,['Positioning','Speed','Bravery'],{roles:['DL','DC','DR','DMC']});m.DribblingSupport=metric(starters,['Dribbling']);m.FitnessMean=metric(starters,['Fitness']);m.TacklingMean=metric(starters,['Tackling']);m.PositioningMean=metric(starters,['Positioning'],{roles:['DL','DC','DR','DMC','ML','MC','MR']});m.BraveryMean=metric(starters,['Bravery']);m.AggressionMean=metric(starters,['Aggression']);
    // Bible's lane score is "relevant attacking/build attributes". This explicit companion implementation
    // uses the union of the named build/attacking attributes already defined in §11.2, not a hidden game weight.
    const laneAttrs=['Passing','Creativity','Dribbling','Crossing','Shooting','Finishing','Speed'];m.LeftLane=metric(starters,laneAttrs,{lane:'left'});m.CenterLane=metric(starters,laneAttrs,{lane:'center'});m.RightLane=metric(starters,laneAttrs,{lane:'right'});return m;
  }
  function rawSupports(m){const av=(...x)=>mean(x);return{
    passing:{short:m.TechnicalBuild,long:av(m.Transition,m.AerialTarget),mixed:av(m.TechnicalBuild,av(m.Transition,m.AerialTarget))},
    shooting:{sight:m.ShootingPower,box:av(m.TechnicalBuild,m.Finishing),balanced:av(m.ShootingPower,av(m.TechnicalBuild,m.Finishing))},
    focus:{left:m.LeftLane,right:m.RightLane,both:av(m.LeftLane,m.RightLane),center:m.CenterLane,balanced:av(m.LeftLane,m.CenterLane,m.RightLane)},
    cross:{low:m.TechnicalBuild,high:av(m.WideAttack,m.AerialTarget),medium:av(m.TechnicalBuild,av(m.WideAttack,m.AerialTarget))},
    lost:{counterPress:m.PressingUnit,regroup:m.DefensiveUnit},won:{buildup:m.TechnicalBuild,counter:m.Transition},
    // Marking matchup effectiveness requires external attack context that this app does not collect.
    // Keep raw own-team support equal; the v2 companion layer uses only own-player feasibility and drain.
    marking:{man:m.DefensiveUnit,zonal:m.DefensiveUnit},pressing:{low:m.DefensiveUnit,mid:av(m.DefensiveUnit,m.PressingUnit),high:m.PressingUnit},
    backLine:{track:m.DefensiveUnit,offside:m.LineControl},tackling:{stay:av(m.TacklingMean,m.PositioningMean),aggressive:av(m.TacklingMean,m.BraveryMean,m.AggressionMean),balanced:av(av(m.TacklingMean,m.PositioningMean),av(m.TacklingMean,m.BraveryMean,m.AggressionMean))}
  };}
  function optionFits(starters,approachKey){
    const approach=B.APPROACHES[approachKey]||B.APPROACHES.balanced,m=lineupMetrics(starters),raw=rawSupports(m),fits={};
    for(const dim of SEARCH_DIMS){
      const vals=B.TACTICS[dim].map(o=>raw[dim][o.key]),hi=Math.max(...vals),fitWindow=Math.max(1,Number(STRAT.CONFIG?.feature_model?.skill_reference?.tactic_option_full_separation_points)||25),normalised=B.TACTICS[dim].map(o=>hi<=0?1:Math.max(0,Math.min(1,1-(hi-raw[dim][o.key])/fitWindow))),mx=Math.max(...normalised);
      fits[dim]={};
      B.TACTICS[dim].forEach((o,i)=>{const squadFit=normalised[i];fits[dim][o.key]={rawSupport:raw[dim][o.key],squadFit,optionFit:squadFit,maxSquadFit:Math.abs(squadFit-mx)<=1e-12};});
    }
    const supportTieMargin=Math.max(0,Number(STRAT.CONFIG?.feature_model?.skill_reference?.comparison_margin_points)||5),equalSupportDims={};
    for(const dim of SEARCH_DIMS){const rows=B.TACTICS[dim].map(o=>Number(raw[dim][o.key])||0),lo=Math.min(...rows),hi=Math.max(...rows);equalSupportDims[dim]=hi-lo<=supportTieMargin+1e-12;}
    const L=raw.focus.left,R=raw.focus.right,C=raw.focus.center,focusSemantic=Math.max(L,R,C)-Math.min(L,R,C)<=supportTieMargin+1e-12?'balanced':Math.abs(L-R)<=supportTieMargin+1e-12&&Math.min(L,R)>C+supportTieMargin?'both':null;
    return{approach,metrics:m,raw,fits,semanticStatic:{equalSupportDims,focusSemantic}};
  }
  const NEUTRAL_TIES={passing:'mixed',shooting:'balanced',focus:'balanced',cross:'medium',lost:'regroup',won:'buildup',marking:'zonal',pressing:'mid',backLine:'track',tackling:'balanced'};
  function near(a,b,eps=1e-9){return Math.abs(a-b)<=eps;}
  function clamp(v,lo,hi){return Math.max(lo,Math.min(hi,v));}
  function neutralCount(values){let n=0;for(const [dim,key] of Object.entries(NEUTRAL_TIES))if(values?.[dim]===key)n++;return n;}
  function semanticTieScore(values,calc,starters){
    let score=0;const signals=[];const add=(condition,reason,points=1)=>{if(condition){score+=points;signals.push(reason);}};
    for(const [dim,key] of Object.entries(NEUTRAL_TIES))add(!!calc.semanticStatic?.equalSupportDims?.[dim]&&values[dim]===key,`${dim}: neutral fallback on equal XI support`);
    const focusSemantic=calc.semanticStatic?.focusSemantic;if(focusSemantic==='balanced')add(values.focus==='balanced','focus: all lanes equal -> balanced',3);else if(focusSemantic==='both')add(values.focus==='both','focus: both flanks equally strongest',3);
    const own=calc.ownBase||STRAT.ownFeatures(starters||[]);
    add(values.won==='counter'&&values.passing==='long'&&own.counterOutletStrong,'direct passing coheres with a strong counter transition');
    add(values.won==='buildup'&&values.passing==='short'&&own.technicalBuild==='high','short passing coheres with a strong technical buildup');
    add(['left','right','both'].includes(values.focus)&&['medium','high'].includes(values.cross),'flank focus coheres with crossing');
    return{score,signals};
  }
  function bucketScore(raw,negativeCap,positiveCap,max,neutral=max/2){raw=Number(raw)||0;if(raw>=0){const cap=Math.max(1,Number(positiveCap)||1);return neutral+(max-neutral)*Math.min(1,raw/cap);}const cap=Math.max(1,Number(negativeCap)||1);return neutral*Math.max(0,1-Math.min(1,Math.abs(raw)/cap));}
  function minimumRawDrain(overrides={},profile=DRAIN_PROFILE_LEGACY){if(profile===DRAIN_PROFILE_LIVE)return null;let raw=15;for(const dim of DIMENSIONS){const vals=B.TACTICS[dim].map(o=>resolveDrainContribution(o.drain,overrides?.[dim]?.[o.key]));raw+=Math.min(...vals);}return raw;}
  function maximumRawDrain(profile=DRAIN_PROFILE_LEGACY){return profile===DRAIN_PROFILE_LIVE?null:100;}
  function liveDrainCeiling(){return null;}
  function drainEfficiency(rawScore,drainLimit,overrides={},profile=DRAIN_PROFILE_LEGACY,liveThresholds=null){if(profile===DRAIN_PROFILE_LIVE)return 5;const ceiling=({Low:40,Medium:65,High:100}[drainLimit]??65),floor=minimumRawDrain(overrides,profile);if(rawScore<=floor)return 10;if(rawScore>=ceiling||ceiling<=floor)return 0;return 10*(ceiling-rawScore)/(ceiling-floor);}
  function withinDrainLimit(drain,drainLimit,profile,liveThresholds){if(profile===DRAIN_PROFILE_LIVE)return drainLimit==='High';return DRAIN_RANK[drain.drainClass]<=(DRAIN_RANK[drainLimit]??1);}
  function scoreDecision(values,drain,calc,starters){
    const fitVals=SEARCH_DIMS.map(dim=>calc.fits[dim][values[dim]]),tacticFit=mean(fitVals.map(x=>x.optionFit)),maxCount=fitVals.filter(x=>x.maxSquadFit).length,contextFit=STRAT.scoreTacticContext(values,starters,calc.ownBase,calc.ruleContext),b=contextFit.buckets||{};
    const decisionComponents={nativeLineupFitScore:tacticFit*32,ownSquadStructureScore:bucketScore(Number(b.own_squad_structure)||0,13,31,26,13),internalCoherenceScore:bucketScore(Number(b.internal_coherence)||0,10,26,18,9),playstyleAndSAScore:bucketScore(Number(b.playstyle_and_sa_fit)||0,1,19,14,0),drainEfficiencyScore:drainEfficiency(drain.rawScore,calc.drainLimit,calc.overrides,calc.drainProfile,calc.liveThresholds)};
    const decisionScore=Object.values(decisionComponents).reduce((a,x)=>a+x,0),contradictionCount=(contextFit.contributions||[]).filter(x=>x.points<0).length,semantic=semanticTieScore(values,calc,starters);
    return{fitVals,tacticFit,maxCount,contextFit,decisionComponents,decisionScore,contradictionCount,semantic};
  }
  function recommend(starters,{approach='balanced',drainLimit='Medium',overrides={},drainProfile=DRAIN_PROFILE_LEGACY,liveThresholds=null}={}){
    if(!starters?.length)return{error:'no-lineup'};const completeness=lineupCompleteness(starters);if(!completeness.complete)return{error:'incomplete-lineup-data',missing:completeness.missing};if(drainProfile===DRAIN_PROFILE_LIVE&&drainLimit!=='High')return{error:'live-drain-thresholds-unresolved',drainProfile,drainLimit};const calc=optionFits(starters,approach);calc.drainLimit=drainLimit;calc.overrides=overrides;calc.drainProfile=drainProfile;calc.liveThresholds=liveThresholds;calc.ownBase=STRAT.ownFeatures(starters);calc.ruleContext=STRAT.prepareTacticRuleContext(starters,calc.ownBase);const mentality=calc.approach.mentality;let best=null,ordinal=0,eligibleCandidates=0;
    const evaluatedCandidates=enumerate(values=>{const drain=calculateDrain(values,overrides,drainProfile,liveThresholds);if(!withinDrainLimit(drain,drainLimit,drainProfile,liveThresholds)){ordinal++;return;}eligibleCandidates++;const sc=scoreDecision(values,drain,calc,starters),cand={values:{...values},...drain,tacticFit:sc.tacticFit,maxSquadFitDimensions:sc.maxCount,semanticTieScore:sc.semantic.score,semanticSignals:sc.semantic.signals,contextFit:sc.contextFit,decisionComponents:sc.decisionComponents,decisionScore:sc.decisionScore,contradictionCount:sc.contradictionCount,neutralChoices:neutralCount(values),ordinal};
      if(!best){best=cand;}else{
        const semanticBand=Math.max(0,Number(STRAT.CONFIG?.tactics?.semantic_near_tie_points)||0.5),scoreDelta=cand.decisionScore-best.decisionScore;
        if(scoreDelta>semanticBand+1e-12)best=cand;
        else if(Math.abs(scoreDelta)<=semanticBand+1e-12){
          if(cand.contradictionCount<best.contradictionCount)best=cand;
          else if(cand.contradictionCount===best.contradictionCount){
            if(cand.semanticTieScore>best.semanticTieScore)best=cand;
            else if(cand.semanticTieScore===best.semanticTieScore){
              if(scoreDelta>1e-12)best=cand;
              else if(near(cand.decisionScore,best.decisionScore)){if(calc.drainProfile===DRAIN_PROFILE_LIVE){if(liveDrainDominatesCheaper(cand,best))best=cand;else if(!liveDrainDominatesCheaper(best,cand)&&(cand.neutralChoices>best.neutralChoices||cand.neutralChoices===best.neutralChoices&&cand.ordinal<best.ordinal))best=cand;}else if(cand.rawScore<best.rawScore||cand.rawScore===best.rawScore&&(cand.neutralChoices>best.neutralChoices||cand.neutralChoices===best.neutralChoices&&cand.ordinal<best.ordinal))best=cand;}
            }
          }
        }
      }
      ordinal++;},mentality);
    if(!best)return{error:'no-drain-compatible-plan',evaluatedCandidates,eligibleCandidates};const reasons={};
    for(const dim of SEARCH_DIMS){const o=option(dim,best.values[dim]),f=calc.fits[dim][o.key],effectiveContribution=drainProfile===DRAIN_PROFILE_LIVE?liveDrainContribution(dim,o.key):resolveDrainContribution(o.drain,overrides?.[dim]?.[o.key]);reasons[dim]=drainProfile===DRAIN_PROFILE_LIVE?`${o.label}: lineup support ${f.rawSupport.toFixed(1)}, squad fit ${(f.squadFit*100).toFixed(0)}%; live drain intensity ${['Low','Medium','High'][effectiveContribution]}.`:`${o.label}: lineup support ${f.rawSupport.toFixed(1)}, squad fit ${(f.squadFit*100).toFixed(0)}%; drain contribution ${effectiveContribution}.`;}
    reasons.marking+=` Matchup-specific marking efficacy is intentionally not modelled; this score uses only own-player feasibility, internal coherence and drain.`;reasons.mentality=`${option('mentality',mentality).label} is locked by the ${calc.approach.label} approach.`;
    return{model:MODEL_VERSION,evidence:'TOP ELEVEN TOOL CALCULATION',evidencePolicy:drainProfile===DRAIN_PROFILE_LIVE?'Live-observed Low/Medium/High option intensities + own-XI attribute fit + transparent v2 own-squad rules. Numeric drain weighting remains neutral until the current server mapping is recovered.':'Historical/default build drain profile + own-XI attribute fit + transparent v2 own-squad rules. No external-team or live-state inputs.',approach:calc.approach.label,approachKey:approach,drainLimit,drainProfile,metrics:calc.metrics,evaluatedCandidates,eligibleCandidates,...best,reasons};
  }
  function recommendAuto(starters,{drainLimit='Medium',overrides={},drainProfile=DRAIN_PROFILE_LEGACY,liveThresholds=null}={}){if(!starters?.length)return{error:'no-lineup'};const completeness=lineupCompleteness(starters);if(!completeness.complete)return{error:'incomplete-lineup-data',missing:completeness.missing};const rows=Object.keys(B.APPROACHES).map(approach=>recommend(starters,{approach,drainLimit,overrides,drainProfile,liveThresholds})).filter(x=>!x.error);rows.sort((a,b)=>b.decisionScore-a.decisionScore||a.contradictionCount-b.contradictionCount||a.rawScore-b.rawScore||b.neutralChoices-a.neutralChoices||a.approachKey.localeCompare(b.approachKey));if(!rows.length)return{error:'no-drain-compatible-plan'};const numericBest=rows[0],own=STRAT.ownFeatures(starters),band=Math.max(0,Number(STRAT.CONFIG?.tactics?.auto_approach_near_tie_points)||1.5),preferred=own.attackDefenseDelta>own.margin?'attacking':own.attackDefenseDelta<-own.margin?'defending':'balanced',preferredRow=rows.find(x=>x.approachKey===preferred),best=preferredRow&&preferredRow.decisionScore>=numericBest.decisionScore-band-1e-12?preferredRow:numericBest,ordered=[best,...rows.filter(x=>x!==best)];return{...best,autoSelected:true,autoApproachCalibration:{numericBest:numericBest.approachKey,preferredByOwnSquad:preferred,nearTieBand:band,attackDefenseDelta:own.attackDefenseDelta,preferenceApplied:best!==numericBest},alternatives:ordered.slice(1,3).map(x=>({approach:x.approach,approachKey:x.approachKey,decisionScore:x.decisionScore,values:x.values,drainClass:x.drainClass,contextFit:x.contextFit}))};}
  TE.Tactics={MODEL_VERSION,DIMENSIONS,SEARCH_DIMS,DRAIN_PROFILE_LEGACY,DRAIN_PROFILE_LIVE,LIVE_DRAIN_MAX,calculateDrain,calculateLegacyDrain,calculateLiveDrain,classifyLiveObserved,liveDrainContribution,liveDrainCounts,liveDrainDominatesCheaper,resolveDrainContribution,enumerate,drainChecksums,lineupCompleteness,lineupMetrics,rawSupports,optionFits,semanticTieScore,minimumRawDrain,maximumRawDrain,drainEfficiency,recommend,recommendAuto,option};
})();
