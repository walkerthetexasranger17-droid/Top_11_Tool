# Build 30527 Machine Index

This directory is intentionally machine-readable. Do not treat it as user configuration.

- `findings.json` — evidence-status register
- `mentor_effects.json` — captured Mentor state + decoded build-localisation semantics
- `tactics_semantics.json` — exact drain + shipped tactic tooltips
- `native_methods.json` — high-value recovered native addresses
- `protocol_fields.json` — high-value protocol field map
- `source_manifest.json` — source/build hashes

Human explanations live in `docs/research/build_30527/`.
